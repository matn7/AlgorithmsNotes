**Interview Tips / Clarify questions**

- Establish data structures like (BST)

```java
class Node {
    int value;
    Node left;
    Node right;
}
```

- Whether number in arrays are unique.
- Whether array is sorted (if so use binary search)
- A custom comparator in sort
